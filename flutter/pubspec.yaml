name: annytunes
description: "Anytone 878UV radio configuration utility"
publish_to: 'none'
version: 1.3.0+3

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^15.1.2
  usb_serial: ^0.5.2
  file_picker: ^8.1.7
  csv: ^6.0.0
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
